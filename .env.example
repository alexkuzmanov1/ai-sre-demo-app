PORT=3000
# Default local dev target. Put your REAL connection string (e.g. Atlas) in .env — it is git-ignored.
MONGODB_URI=mongodb://localhost:27017/shop
# Atlas example (set the real value in .env, never commit it here):
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/shop?retryWrites=true&w=majority
REDIS_HOST=localhost
REDIS_PORT=6379
RESPONDER_WEBHOOK_URL=http://localhost:4000/api/incidents
SERVICE_NAME=mini-shop
